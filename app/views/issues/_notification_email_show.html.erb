<%= issue_fields_rows do |rows|
  unless @issue.disabled_core_fields.include?('due_date')
      rows.right l(:field_recipient_email), TechMailer.extract_email_to_array(@issue.recipient_email).join(', '), :class => ':recipient_email'
  end
end %>
